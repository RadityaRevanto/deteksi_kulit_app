class SkinType {
  final String uuid;
  final String name;
  final String description;
  final bool isActive;

  const SkinType({
    required this.uuid,
    required this.name,
    required this.description,
    required this.isActive,
  });
}
